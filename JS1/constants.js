const host = "https://api.noroff.dev";
const endpoint = "/api/v1/gamehub/";
export const apiUrl = host + endpoint;

const oldHost = "https://api.noroff.dev";
const oldEndpoint = "/api/v1/old-games";
export const oldApiUrl = oldHost + oldEndpoint;